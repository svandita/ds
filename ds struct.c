#include<stdio.h>
struct node
{
    void append(struct noode **,int);
    void addatbeg(struct noode **,int);
    void addafter(struct noode **,int,int);
    void display(struct node *);
    int count(struct node *);
    void del(struct node **,int);
    int mak





};

